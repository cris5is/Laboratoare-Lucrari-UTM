#include <stdio.h>
#include <string.h>
char sum;
int main(char *string_1, char *string_2)
{
    char *string_1, *string_2;
    int len_1 = strlen(string_1), len_2 = strlen(string_2);
    scanf("%s", string_1);
    scanf("%s", string_2);

    return 0;
}
